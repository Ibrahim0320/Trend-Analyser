{
  "name": "trend-analyser",
  "private": true,
  "scripts": {
    "dev": "cd client && npm run dev",
    "vercel-build": "npm ci && cd client && npm ci && npm run build"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "crypto-js": "^4.2.0",
    "fontkit": "^2.0.2",
    "pdfkit": "^0.14.0",
    "png-js": "^1.0.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  },
  "engines": {
    "node": ">=18.17 <23"
  }
}
